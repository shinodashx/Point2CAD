interface:
  display_name: "点云 → CadQuery 序列重建（中文）"
  short_description: "统一分析 NPY、PLY、XYZ，以 CAD 特征序列重建并量化精度"
  default_prompt: "使用 $reconstructing-cadquery-from-point-clouds-zh，以 XYZ 测量为主、多视图为辅，建立证据和操作计划，输出独立 CadQuery 序列、STEP 与迭代验收报告。"
