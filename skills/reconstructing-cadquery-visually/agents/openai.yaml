interface:
  display_name: "点云 → CadQuery（纯可视化 / Visual-only）"
  short_description: "仅看点云多视图，保留序列建模、圆角接合、迭代和 CAD 质量检查"
  default_prompt: "使用 $reconstructing-cadquery-visually 将我的 NPY、PLY 或粘贴 XYZ 仅用于绘点显示，从实际多视图理解并重建独立 CadQuery 序列；不做点云数值分析或距离优化，保留现有 CAD 操作和验收规则。"
