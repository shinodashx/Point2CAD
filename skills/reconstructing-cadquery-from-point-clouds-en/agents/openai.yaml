interface:
  display_name: "Point Cloud → CadQuery Sequence Reconstruction (English)"
  short_description: "Reconstruct CAD sequences with XYZ-led analysis and validation"
  default_prompt: "Use $reconstructing-cadquery-from-point-clouds-en to lead with XYZ measurement and support it with multiple views, establish evidence and an operation plan, and output a standalone CadQuery sequence, STEP, and iterative acceptance report."
