interface:
  display_name: "Point Cloud → CadQuery Sequence (English)"
  short_description: "Analyze NPY/PLY/XYZ and rebuild verified CAD feature sequences"
  default_prompt: "Use $reconstructing-cadquery-from-point-clouds-en to analyze my point cloud, establish dimension evidence and a CAD operation plan, then deliver a standalone CadQuery reconstruction sequence and measured errors; do not convert meshes or wrap the cloud."
